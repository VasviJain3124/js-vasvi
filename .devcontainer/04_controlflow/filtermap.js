const myNums = [1,2,3,4,5,6,7,8,9]

const myNewNums = myNums.map((num)=> num+10)// agar hum paranthesis ki jagah curly braces lga denge to it will not run until we will write return infront of num+10

console.log(myNewNums);

